using UnityEngine;

namespace AbilitySystem {
    /// <summary>
    /// Key for base entity animator controller to call specific animations with ease.
    /// </summary>
    public enum AnimationType {
        TransformIn,
        TransformOut,
        Attack1,
        Death,
    }
}